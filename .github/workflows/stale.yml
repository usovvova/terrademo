<<<<<<< HEAD
name: 'Stale issues'
run-name: Cleaning up issues ${{ github.run_number }}
on: 
  schedule:
    - cron: '0 0 */15 * *' # Every 15 days, starting from 1 at 00:00
# Every Friday at 00:00
    

permissions:
  issues: write
jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v6
        with:
          days-before-issue-stale: 45
          days-before-issue-close: 15
          stale-issue-label: 'Stale'
          stale-issue-message: >
                      This issue has been automatically marked as stale because it has not had
                      recent activity. It will be closed if no activity occurs on the next 15 days.
                      Thank you!
          close-issue-message: >
                      This issue was closed automatically after 60 days of inactivity Thank you!
          remove-issue-stale-when-updated: 'true'


=======
name: 'Close issues on a cron schedule'
on:
  schedule:
    - cron: '30 1 * * *'

permissions:
  issues: write
jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v6
        with:
          days-before-issue-stale: 60
          days-before-issue-close: 7
          stale-issue-label: 'stale'
          stale-issue-message:
          'This issue has been automatically marked as stale because it has not had
          recent activity. It will be closed if no further activity occurs. Thank you
          for your contributions.'

          debug-only: 'true'
          remove-issue-stale-when-updated: 'true'
          # debug-only: 'true'
>>>>>>> parent of 8b63ecb... Correction of stale.yaml syntax.
